# aish プロジェクト共有設定。
# sandbox_mode は経路ごとに分離:
#   CLI: 呼び出し側で指定（sandbox backend は scripts/codex-cli.sh が選択）
#   MCP: scripts/codex-mcp-wrapper.sh が workspace-write + network off に固定
# task ごとの推論強度は MCP の config 引数で明示する。
# 正本: docs/codex-delegation.md

[sandbox_workspace_write]
network_access = false
